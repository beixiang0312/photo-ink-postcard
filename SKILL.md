---
name: photo-ink-postcard
description: 将一张或多张城市、建筑、铁路、桥梁、江河、乡村或自然风景照片制作成竖版 3:4 摄影水墨画册明信片：上半区严格保留真实摄影，下半区生成来源对应的极简东方水墨重构，并通过确定性流程添加用户指定的动态主标题与本地英文短句次标题。适用于“摄影水墨明信片”“摄影与水墨上下对照”“东方建筑风景研究版式”“城市或地点明信片”及批量处理图片目录。主标题必须根据用户输入动态设置，不得默认使用任何城市名称；次标题只能从技能内置的本地分类英文短句库选取，禁止联网搜索或核实名言。
---

# 通用摄影水墨明信片

制作克制、安静的 3:4 米白色画册明信片。上半区忠实保留输入照片，下半区呈现从原图提炼的极简水墨研究，最后通过确定性排版加入用户指定的主标题和本地英文短句。

## 必需输入

- 输入图片或图片目录。
- 主标题精确文字。主标题是动态参数，可以是城市、地点、主题、作品名或用户指定的其他文字，例如 `Beijing, China`、`Suzhou, Jiangsu`、`江南水乡`。
- 用户没有提供主标题时，先从当前请求、目录名、文件元数据或明确地点信息中推断。无法可靠确定时，必须询问用户，不得默认使用任何城市名称。

## 强制规则

- 禁止写死或默认使用任何地点、城市或主题标题。
- 禁止为了次标题联网搜索、核实、署名或引用来源。
- 读取 `references/quote-library.json`，使用 `scripts/select_quotes.py` 从本地选择英文短句。
- 名言库总量不得超过 100 条。每次修改后运行 `scripts/validate_library.py`。
- 名言库只保存并呈现英文正文。不得添加作者、出处、引号、变量、花括号或检索说明。
- 保留所有原始图片，任何结果都写入新路径。
- 不得让图像模型绘制上半区真实照片、主标题或次标题。
- 图像生成使用 `codex-image2` 技能。调用其命令行工具前，先完整读取该技能的 `SKILL.md`。
- 原图、水墨底图、动态主标题和次标题必须通过确定性流程合成。

## 工作流程

1. 确定主标题精确文字，并在整个批次中保持一致；如果用户为不同图片指定不同标题，则建立逐图映射。
2. 递归清点输入图片。支持 `.jpg`、`.jpeg`、`.png`、`.webp`、`.bmp`、`.tif`、`.tiff`。
3. 检查每张照片，识别主要景物、空间层级、视觉轴线、透视关系、视觉重量和低饱和度配色。
4. 根据照片情绪选择短句类别。读取 `references/quote-styles.md`。
5. 每张图片选择一条不重复的本地英文短句。批量选择示例：

   ```powershell
   python scripts/select_quotes.py --style journey --count 3 --seed "batch-name"
   ```

   多类均适用时使用 `--style auto`。选中的精确文字只传给后续排版，不让图像模型绘制。
6. 读取 `references/prompt-template.md`。根据照片填写所有方括号字段，保存提示词前不得残留任何占位符。
7. 使用原照片作为参考，通过 `codex-image2 edit` 只生成横版水墨下半区。优先使用 `1536x1024`、`quality high`。
8. 检查生成底图：必须对应原图空间，形体应概括、边缘柔和、无可读文字、无新增主要景物。只重新生成不合格底图。
9. 使用 `scripts/compose_postcard.py` 合成最终 1536×2048 PNG。脚本仅等比例缩放上半区原图，柔化生成水墨底图的纸张边缘，并确定性添加动态主标题和次标题。
10. 批量任务制作联系表进行视觉检查。确认严格 3:4、主要景物完整、上下分区清楚、主标题与用户输入逐字一致、次标题准确、底部安全边距充足，并且水墨下半区不存在多余可读文字。

## 主标题处理

- 将主标题视为必须显式传递给合成脚本的精确文字。
- 保留用户提供的拼写、标点、大小写与语言，不擅自翻译或改写。
- 标题过长时由合成脚本自动缩小字号；不得截断、换词或省略。
- 主标题可以是中文或英文。脚本会根据字符覆盖范围选择适合的字体。
- 同一批次主标题统一时，只需确定一次；逐图标题不同时，必须逐一传入。

## 短句选择

- 优先选择 4—9 个英文单词、语义完整且积极的短句。
- 根据画面情绪选择，不必机械对应具体物体名称。
- 同一批次不得重复使用同一句。
- 指定类别可用短句不足时，依次回退到 `journey`、`future`、`hope`、`calm-resilience`。
- 将库内内容视为不署名的编辑性英文短句，不得宣称由某位名人创作。

## 确定性合成

运行：

```powershell
python scripts/compose_postcard.py `
  --photo "input.jpg" `
  --panel "generated-panel.png" `
  --title "Custom exact title" `
  --subtitle "Selected local phrase" `
  --out "output-postcard.png"
```

默认输出为 1536×2048。`--title` 为必填参数，不存在默认城市标题。

上半区照片必须采用完整容纳式等比例缩放，不得为了铺满宽度而裁切。无法被照片覆盖的区域使用哑光米白色。不得调色、锐化、美化、修饰、移除、增加、复制或移动照片内容。

## 名言库维护

缓慢、审慎地增加短句。保持稳定且唯一的 ID，每条只属于一个类别，只保存英文正文，不增加署名字段。新增前：

1. 如果库中已有 100 条，必须先删除或替换一条，不得突破上限。
2. 保证文字不重复、英文自然、长度为 4—9 个单词，且没有首尾引号。
3. 运行 `python scripts/validate_library.py`。

维护和生成过程均不得使用网络查询名言。
